import test;

export function test_assert() {
    test.assert(!false);
    test.assert(null == pcall(function () {
        test.assert(false);
    }), "Failed asserts raise errors");
}

test_assert();